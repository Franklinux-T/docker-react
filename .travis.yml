language: generic
sudo: required
services:
    - docker 

before_install:
    - docker build -t franklinux-t/docker-react -f Dockerfile.dev .

script: 
    - docker run -e CI=true franklinux-t/docker-react npm run test 